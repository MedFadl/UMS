spring:
  application:
    name: UMS

  datasource:
    url: jdbc:mariadb://localhost:3306/UMS
    username: root
    password: admin
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

